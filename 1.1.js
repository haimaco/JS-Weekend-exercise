# JS-Weekend-exercise
//1.1 YES OR NOT :
//Complete the method that takes a boolean value and return a "Yes" string for true, or a "No"
//string for false.

const booleanvalue =(bool)=>
{
        let boolean ;
        bool ? boolean = "Yes" : boolean = "No";
        return boolean;
    
};

console.log(booleanvalue(false));
